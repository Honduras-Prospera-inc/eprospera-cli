---
"@prospera/eprospera-cli": patch
---

Let one-off Agent Keys from `--api-key` and `EPROSPERA_API_KEY` defer scope authorization to the API when no cached scope metadata is available. Release versioning now also keeps the OpenCLI schema version in sync with the package version.
